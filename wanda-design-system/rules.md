# Wanda — правила роботи з Figma-лейаутами

## Виключно дизайн-система Wanda

Непотрібно ходити по іншим дизайн системам щоб не втрачати час — ми працюємо виключно з дизайн-системою Wanda і скілами `wanda-design-system`.

## Брейкпоінти (xs / sm / md / lg / xl)

1. ~~Горизонтальна відстань між сусідніми фреймами брейкпоінтів — завжди рівно 200px~~ — **виправлено 03.08.2026.** Пряме вимірювання нової збірки "Light — Duel arena" (file `rdQH0d2MRRKFkgzCmCGLGw`, node `6002:20991`, усі 5 брейкпоінтів xs→sm→md→lg→xl) показало **рівно 80px** між кожною сусідньою парою фреймів, без винятку. Це та сама дистанція, що вже задокументована нижче в чеклисті "Підготовка макетів" як "Дистанція 80px між макетами/локальними компонентами всередині секції" — брейкпоінт-фрейми одного макета якраз і є ці "локальні компоненти всередині секції". Число 200px більше не підтверджується жодним живим прикладом — не використовувати.
2. Коли рядок/грід (напр. Stats row, Cards) переводиться на `layoutWrap="WRAP"` для рефлоу під вужчі брейкпоінти —
   обов'язково задавати `counterAxisSpacing`, рівним `itemSpacing` (гепу між колонками).
   Інакше рядки, що переносяться, злипаються без відступу по вертикалі.

## Технічне ревью (лейаутів/брейкпоінтів) — визначення процедури

Усе, що описано в розділах "Сторінкові лейаути", "Візуальні баги, знайдені при аналізі лейаутів", "Процес аналізу лейауту" і "Нейминг шарів" нижче — це **одна процедура з однією назвою: Технічне ревью**. Це та сама стадія "⚪️ Чистовик — Technical review" зі стадій роботи з документами (розділ "Процес роботи з документами" нижче). Коли хтось просить "зроби технічне ревью" на лейауті/сторінці/секції — виконується саме ця послідовність:

1. Визначити breakpoint і тип сторінки → звірити з еталонним прикладом (`references/pages/*.md`, поки що лише Main Page) і таблицями з розділу "Сторінкові лейаути" нижче.
2. Перевіряти не лише resolved-числа, а фактичні `boundVariables` на нодах (padding/gap/sizing) — resolved число може випадково збігтись з правильним, а `boundVariables` показує, чи це справжній токен, хардкод, чи прив'язка до НЕправильної змінної (як було з `layout/gap/secondary` vs `card/duel/gap`).
3. Кожне підтверджене відхилення — записати в розділ "Візуальні баги, знайдені при аналізі лейаутів" за форматом: компонент/сторінка → брейкпоінт → відхилення → з чим звіряли.
4. Якщо відхилення спростовується повторною перевіркою (як Container-padding чи Card/Duel-gap вище) — не видаляти запис, а позначати `~~закреслено~~` + причина, щоб later-сесії не наступали на ту саму помилку.
5. Виправлення — за процесом "Процес аналізу лейауту — завжди з виправленою версією" (копія поруч з оригіналом, оригінал не чіпати, дати обидва посилання), якщо користувач прямо не попросив правити в оригіналі.
6. Проактивно перейменувати всі автолейаут-шари на `Wrapper` (розділ "Нейминг шарів" нижче) — без окремого запиту.

## Сторінкові лейаути (page-level grid) — на основі аналізу "Lobby — Authorized"

Еталонна збірка: файл `QbmTYjRSyzALRb5GIl4Itk` ("W" — Main), node `8991:58214`, усі 5 брейкпоінтів (xs/sm/md/lg/xl).

### Вертикальний ритм — залежить від брейкпоінта (перевірено повторно 30.07.2026 на 15+ парах секцій підряд)
1. Гап між сусідніми секціями сторінки (Wrapper→Wrapper): **16px на xs/sm/md**, **24px на lg/xl**. Межа переходу — між md і lg, та сама, що й для Header-розміру.
2. Гап Heading → контент секції: **8px** на xs/sm/md, **12px** на lg/xl (heading при цьому росте з 32px до 40px).
3. Top padding контейнера перед першою секцією: xs=4px, sm=8px, md/lg/xl=16px.

### Горизонтальні падінги — НЕ масштабуються лінійно з шириною! Завжди звіряти по факту, не екстраполювати
| Breakpoint | Ширина Page-container | Side padding секції |
|---|---|---|
| xs | 360 | 16px |
| sm | 768 | 40px |
| md | 1024 | 40px |
| lg | 1280 | 36px |
| xl | 1360 | 32px (додатковий шар padding на рівні `Container`, якого нема на md/lg) + 36px на рівні секції |

xl — виняток: тут з'являється додатковий рівень padding на `Container` (32px) поверх стандартного секційного (36px). На md/lg такого немає — там `Container` без бічних падінгів, все падить на рівні секції.

### Гапи всередині карткових рядів — фіксовані 8px, не масштабуються
Card/Activities, event, Card/Tournament, Card/Default — гап між картками = **8px на всіх брейкпоінтах**, незалежно від ширини картки.

### Реальні Figma-змінні за цими числами: `layout/gap/primary` і `layout/gap/secondary` (підтверджено 31.07.2026)
Числа гапів вище — це не просто "правила з голови", а два реальні bound-variable сімейства в колекції `Theme` (файл ДС), підтверджені прямим читанням `boundVariables` на живих нодах "Duel arena" (`v7eYtgEjdUuz5VxjqRSH4h`, усі 5 брейкпоінтів):

| Змінна | xs | sm | md | lg | xl | Де використовується |
|---|---|---|---|---|---|---|
| `layout/gap/primary/{bp}` | 16 | 16 | 16 | 24 | 24 | Гап **між секціями** — на топ-рівні `Container` (та сама цифра, що й "гап між секціями" вище) |
| `layout/gap/secondary/{bp}` | 8 | 8 | 8 | 12 | 12 | Гап **всередині контенту секції** — внутрішні Wrapper'и Card/Default (пошук ігор, ряди Thumb), кластер Text→картковий ряд→Navigator на Duel Arena (не сам картковий ряд — див. рядок нижче) |
| `layout/gap/row/{bp}` | 8 | 8 | 8 | 8 | 8 | **Новий, підтверджено 03.08.2026.** Гап усередині **карткового ряду** (Card/Duel) — фіксовано 8px на всіх брейкпоінтах, НЕ росте до 12 на lg/xl. Підтверджено прямим читанням boundVariables на живих нодах ("Light — Duel arena", file `rdQH0d2MRRKFkgzCmCGLGw`, node `6002:20991`, усі 5 брейкпоінтів) — клас на ноді ряду буквально `gap-[var(--layout/gap/row/lg,8px)]`, не `--layout/gap/secondary`. |

**Ключове правило:** якщо гап — це відступ між двома "великими" блоками контенту сторінки (секціями) → біндь на `layout/gap/primary/{bp}`. Якщо гап — усередині секції, між дрібнішими елементами одного змістового блоку (вкладені Wrapper'и, кластер Text→ряд→Navigator) → біндь на `layout/gap/secondary/{bp}`. Якщо гап — конкретно між картками в картковому ряду → біндь на `layout/gap/row/{bp}` (завжди 8px, не росте). Не хардкодь число — завжди підв'язуй саме цю змінну, це дозволяє темі/брейкпоінту оновлюватись автоматично.

⚠️ **Виправлено 03.08.2026:** запис нижче ("Duels 3.0", п.1) стверджував, що ряд Card/Duel коректно прив'язаний до `layout/gap/secondary/{bp}` (тобто 12px на lg/xl). Пряма перевірка нової збірки в ІНШОМУ файлі — "Light — Duel arena" (`rdQH0d2MRRKFkgzCmCGLGw`, node `6002:20991`) — показала інше: там ряд Card/Duel на кожному брейкпоінті, включно з lg/xl, тримає рівно 8px (не 12), і boundVariable — саме `layout/gap/row/{bp}`, а не `layout/gap/secondary/{bp}`. Обидва виміри залишені нижче (не видалені) — це два різні Figma-файли з, судячи з усього, неузгодженим біндингом токена для одного й того ж типу картки. Перед правкою карткового ряду в конкретному файлі — завжди перевіряй boundVariables саме цього файлу, не екстраполюй з іншого.

### Card/Default — це horizontal-scroll карусель, НЕ grid (виправлено 30.07.2026)
Розмір картки фіксований **200×112 на будь-якому брейкпоінті** (не масштабується), але це **не строгий N-up грід** — контейнер горизонтально скролиться, і кількість видимих карток просто випливає з ширини контейнера ÷ (200+8px gap), а не є навмисною дизайн-специфікацією "рівно N карток на breakpoint". Не фіксуй конкретні числа карток на breakpoint як правило — вони змінюються будь-якою зміною ширини Page-container.

### Загальна структура сторінки (md/lg/xl)
`Header` → `Wrapper` (Sidebar 80px fixed + Page-container) → `Hero` → `Body` → `Container` (top padding) → N × секція `Wrapper` (Heading + контент, гап 16px між секціями) → `Footer`.
На xs/sm сайдбару немає, Header на всю ширину, Hero одразу під Header (без обгортки Wrapper з сайдбаром).

## Додавання нового контенту (Wanda design system)

1. Використовувати ВИКЛЮЧНО компоненти з дизайн-системи — instance реальних компонентів
   через `getMainComponentAsync` / `importComponentSetByKeyAsync` / `search_design_system`.
   Не малювати імітації вручну через fills/rectangles.
2. Кольори — тільки токени/змінні дизайн-системи. Не хардкодити нові hex-кольори
   (в т.ч. не використовувати чорний `#000000`/near-black для нових карток/блоків,
   якщо це не існуючий токен).
3. Шрифти — тільки ті, що вже задані в ДС (Bungee для заголовків, Open Sans для тексту)
   з правильними style-неймами.
4. Увесь текстовий контент (заголовки, лейбли, кнопки, плейсхолдери) — завжди англійською
   мовою, незалежно від мови запиту в чаті.

## Візуальні баги, знайдені при аналізі лейаутів

Коли Claude звіряє готовий лейаут із правилами вище (breakpoint-аналіз) і знаходить відхилення, яке візуально виглядає як **помилка збірки**, а не навмисне рішення — таке відхилення фіксується тут окремим пунктом, а не додається як нове "правило" (бо це не стандарт, а known issue для перевірки/фіксу).

Формат запису: компонент/сторінка → який брейкпоінт → в чому відхилення → з чим звіряли.

### Duels 3.0 (file `v7eYtgEjdUuz5VxjqRSH4h`, node `34861:17423`, секція "Duel arena") — перевірено 30.07.2026

1. ~~**Card/Duel → гап між картками в ряду** → 16px замість 8px~~ — **⚠️ СПРОСТОВАНО 31.07.2026.** Перше вимірювання (16px, по дельтах x/width з `get_metadata`) не збіглось із фактичним `boundVariables`: пряме читання властивостей на живих нодах показало, що ряд Card/Duel коректно прив'язаний до `layout/gap/secondary/{bp}` (8 на xs/sm/md, **12 на lg/xl** — не 8!), тобто це і є правильна поведінка, узгоджена з рештою секції (Card/Default всередині тієї ж "Duel arena" використовує ту саму змінну). Мій попередній "фікс" (rebind на `card/duel/gap`=8 constant) був зайвим — на lg/xl я б цим *ввів* відхилення від 12 до 8. Тестовий вузол з тим фіксом (`34955:...`) уже видалений з файлу, відкочувати нічого не треба. Правило "Гапи всередині карткових рядів = 8px завжди" (вище) до Card/Duel НЕ застосовується — для нього чинне `layout/gap/secondary/{bp}` з таблиці нижче.
2. **Card/Duel → блок "Possible prizes"** → усі брейкпоінти, у всіх 3 картках ("Newbies"/"Legend"/"Pro") → в `Variant=Default` інстансовано **10** `Card/Duel/Chip` замість 5, а контейнер розрахований лише на 1 ряд (40px) → **4-6 чіпів обрізаються overflow**, фізично не рендеряться → звірено з майстер-компонентом у бібліотеці ДС: `Default` там рівно 5 чіпів (влазить без обрізання), для повного списку є окремий `Variant=Expand` (×-кнопка замість chevron, без обмеження в 1 ряд) — див. `references/card/duel.md`. Виправлення: перемкнути на `Expand`, а не дописувати чіпи в `Default`.

⚠️ Перевірено й **спростовано** цього ж дня: "Container padding-top не відповідає 4/8/16/16/16" — хибне порівняння, сам Container не має padding-токена (немає `layout/paddingV/*` у `get_variable_defs`), відступ приходить з іншого ноду. НЕ вважати це відхиленням.

### Light — Duel arena, нова збірка (file `rdQH0d2MRRKFkgzCmCGLGw`, node `6002:20991`) — перевірено 03.08.2026

Це ІНША секція в тому самому файлі, що й еталон `references/pages/duel-arena.md` (там — node `4063:27522`). Порівняння нижче — між цією новою секцією і задокументованими правилами/еталоном, не між двома копіями одного й того ж вузла.

1. **Card/Duel → блок "Possible prizes"** → той самий баг, що й у "Duels 3.0" вище: усі 3 картки, усі 5 брейкпоінтів — інстансовано 10 `Card/Duel/Chip` замість 5, ряд обрізається overflow (контейнер `h-[40px] overflow-clip`). Той самий фікс: перемкнути на `Variant=Expand`, а не дописувати чіпи в `Default`.
2. **Card/Duel → гап у ряду** → підтверджено фіксовано 8px на ВСІХ брейкпоінтах, включно з lg/xl (виміряно дельтами x і підтверджено boundVariable `layout/gap/row/{bp}` напряму в коді ноду, не лише resolved-число). Суперечить запису вище про файл "Duels 3.0" (там той самий тип картки коректно біндиться на `layout/gap/secondary`, що дає 12 на lg/xl) — див. виправлення в розділі "Реальні Figma-змінні" вище.
3. **Card/Duel → ширина картки НЕ фіксована 298px на md/lg/xl** — на цих брейкпоінтах картка розтягнута (`flex-[1_0_0]`, `max-w-[428px]`) так, щоб рівно 3 картки + 2 гепи по 8px заповнювали всю ширину контент-колонки (282.67/370.67/402.67px відповідно на md/lg/xl — підтверджено формулою 3×card+2×8=content width для кожного з трьох брейкпоінтів). На xs/sm картка лишається фіксованою 298px і рядок горизонтально скролиться (не влазить, як і задокументовано). Токен `card/duel/maxWidth:428` раніше не був задокументований у `references/card/duel.md` — новий.
4. **Кластер "Text(опис) → ряд Card/Duel → Navigator" згруповано в один composite Wrapper з внутрішнім гепом `layout/gap/secondary/{bp}`** (8/8/8/12/12, підтверджено на всіх 5 брейкпоінтах) — суперечить `references/pages/duel-arena.md` розділу 2, де стверджувалось, що на Duel Arena взагалі немає окремого тіснішого під-ритму і все йде на макро 16/24. У ЦІЙ збірці — є: макро-гап `layout/gap/primary` між великими блоками (Заголовок-блок ↔ цей composite Wrapper ↔ Card-пікер), і мікро-гап `layout/gap/secondary` усередині цього Wrapper.
5. Горизонтальна дистанція між фреймами брейкпоінтів — **80px**, не 200px (див. виправлення розділу "Брейкпоінти" вище, підтверджено саме на цій збірці).

Пункти 2-4 — не обов'язково "помилки збірки" в сенсі негативного дефекту; це підтверджені структурні відмінності нової збірки від того, що було задокументовано з попередньої (`node 4063:27522`). Записано тут за тим самим форматом, що й known issues, щоб наступна сесія звіряла з актуальним станом, а не зі старим описом.

## Процес аналізу лейауту — завжди з виправленою версією

Коли Claude робить аналіз готового лейауту на відповідність правилам (breakpoint-звірка) і знаходить відхилення/баг:
1. Не обмежуватись текстовим описом — одразу створити в тому ж Figma-файлі (поруч із оригінальним фреймом, з відступом, без зміни оригіналу) виправлену копію проблемного фрейму/секції з застосованим фіксом.
2. Дати обидва nodeId/посилання — оригінал і виправлений варіант — щоб користувач міг порівняти візуально (напр. через скрін або side-by-side).
3. Текстовий опис (що саме поправлено) додається як підпис до виправленої версії, а не замінює її.

## Нейминг шарів під час технічного ревю

Роблячи технічне ревю лейауту (breakpoint-звірка чи будь-яка інша перевірка макета), Claude одразу перейменовує всі шари-автолейаути на `Wrapper` — не чекаючи окремого запиту користувача і не обмежуючись коментарем "перейменуй тут". Це правило нейминга (див. чеклист "Підготовка макетів" вище), яке застосовується проактивно в межах самого ревю.

## Процес роботи з документами (Чорновик → Чистовик)

### Термінологія
- **⚫️ Чорновик** — окремий документ у проєкті "Worklog". Для попереднього затвердження концепту Дімою/Колею/Замовником. НЕ зобов'язаний мати всі брейкпоінти/стани/компоненти.
- **⚪️ Чистовик** — зміни, перенесені в бренчу відповідного документа з "Sitemap". Зобов'язаний мати всі брейкпоінти, бути зібраним виключно на компонентах/токенах/стилях бібліотеки.

### Стадії роботи з документами
1. ⚫️ Чорновик — Wanda review (внутрішній груммінг/рев'ю командою Wanda)
2. ⚫️ Чорновик — CD/HD review (затвердження концепту, UI/UX)
3. ⚫️ Чорновик — First customer review
4. ⚪️ Чистовик — Technical review
5. ⚪️ Чистовик — Second customer review (фінальне затвердження замовником)

### Перехід на ⚪️ Чистовик
- У відповідному документі з "Sitemap" створюємо бренчу з назвою `Updates [номер]` (у квадратних дужках — тільки номер задачі, без префіксу "YDW-").
- Якщо бренча `Updates [***]` вже існує в цьому документі — переносимо зміни в неї і дописуємо свій номер задачі через слеш (`Updates [123/456]`).

### Підготовка до Technical Review

**Підготовка компонентів**
- Коректний нейминг (Sentence case; Parent/Child)
- Відсутність нецілих значень
- Логічні й інтуїтивні назви властивостей та їх значень
- Правильний порядок властивостей і значень (від частих до рідких, від більшого до меншого)
- Boolean-формат властивості, коли доступно лише 2 значення
- Видалити властивості, у яких доступне лише 1 значення
- Відповідність назви властивості й шару, до якого вона застосовується
- Відповідність порядку властивостей і шарів
- Відсутність граматичних помилок у назвах компонентів/властивостей/значень/шарів/токенів
- Заблокувати шари/instance, до яких треба обмежити доступ
- Коректна відпрацювання всіх комбінацій властивостей
- Коректне "розтягування/звуження" компонента, якщо це передбачено
- Коректна поведінка тексту при вводі довгих слів/словосполучень/речень
- Прив'язка елементів компонента до відповідних токенів (з урахуванням варіантів/станів/форм)
- Відсутність "зламаних" токенів
- Відсутність прив'язок через стиль або змінну (лише токени)
- Перевикористовувати нейминг-патерни раніше створених компонентів
- Автоматична калькуляція значень, де можливо
- Позиціонувати всі компоненти відносно 0-координати
- Вирівняти варіанти всередині component set
- Залишити увімкненими тільки ті властивості, що використовуються за замовчуванням
- ⚠️ Використовувати префікс `v2/v3` у назвах токенів для підтримки старого й нового набору

**Підготовка макетів**
- Підтягнути всі зміни з бібліотек
- Позбутись зайвих бібліотек і модів із них
- Відсутність component-змінних у списку доступних
- Позбутись сміття
- Позиціонувати всі секції відносно 0-координати
- Згрупувати всі локальні компоненти в одну секцію (крім контенту для модалок/дроверів)
- Кожен макет — усі брейкпоінти згруповані в секцію з інтуїтивною назвою
- Підготувати всі стани компонентів (у розумних межах; опційно)
- Дистанція **600px** між секціями
- Дистанція **80px** між макетами/локальними компонентами всередині секції
- Забезпечити макети картинками
- Збірка макетів виключно на токенах/змінних/стилях/компонентах бібліотеки
- Коректний нейминг усіх шарів — усі автолейаут-фрейми називати `Wrapper`
- Відсутність зайвих фреймів/врапперів у макетах
- Відсутність зламаних компонентів
- Безпроблемний своп бібліотек

### Синхронізація / DS Sync (🚨 обрати один варіант)
- Вручну переносимо зміни у разі часткової реалізації задач
- Або призупиняємо синхронізацію макетів до виконання всіх наслоєних задач у бренчі

### FAQ
**Коли видаляти ⚫️ Чорновик?**
Тільки після абсолютної готовності ⚪️ Чистовика — коли всі зміни перенесені. Залишити посилання на ⚪️ Чистовик у самій задачі (в Jira).

**Як зберегти "корисні" напрацювання з ⚫️ Чорновика перед видаленням?**
Створити новий документ, перенести туди все потрібне, і лише тоді видалити чорновик (чорновик ОБОВ'ЯЗКОВО має перестати існувати, щоб уникнути помилок при передачі макетів розробникам).

## Процес оновлення цього файлу

Кожна зміна в rules.md комітиться в цей репозиторій і йде в основну гілку
(за бажанням — через Pull Request для рев'ю). Колеги отримують оновлення через `git pull`.

## Редагування компонентів

Файли компонентів у `references/*.md` (включно з `card/*.md`) — це задокументований опис реального стану дизайн-системи в Figma. Claude НЕ РЕДАГУЄ ці файли самостійно за власною ініціативою. Будь-яка зміна в reference-файлі компонента вноситься лише за прямим запитом користувача з чітким описом, що саме змінилось у Figma (новий варіант, токен, стан тощо), і завжди звіряється з актуальним `get_design_context`/`search_design_system`, а не пишеться "з пам'яті".

# Certificates

Canvas `5:2648`, file `1iLHv8dNR3kwSoBYNl1wgs`. Статичні лого-бейджі довіри/регуляторів — ті самі "Awards", що вбудовані в Trust-блок `hero.md`.

## Компоненти
Кожен — просто SVG-логотип з Theme(Light/Dark), без іншої логіки:
- **DMCA Protection** (`8:140`) — 219×32px.
- **AskGamblers Certificate** (`8:145`) — 96×32px.
- **Slotozilla** (`8:150`) — 96×32px, лише один theme (немає варіанту Light/Dark).
- **SSL** (`8:152`) — 68×32px.
- **GamCare** (`2306:286`) — 96×28px.

## Props
```
Certificate({ theme: "Light"|"Dark" })  // окрім Slotozilla — без пропсів
```

## Notes
- Це чисті лого-асети (запечені SVG), не інтерактивні компоненти — просто вставляй відповідний варіант залежно від того, світла чи темна секція сторінки.
- Той самий набір бейджів, що вже бачили в `hero.md` (Trust-блок, "Awards"-ряд) — не малюй їх заново, використовуй ці готові компоненти.

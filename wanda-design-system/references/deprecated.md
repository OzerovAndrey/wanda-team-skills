# Deprecated

Canvas `5:2651`, file `1iLHv8dNR3kwSoBYNl1wgs`. **Порожній canvas** — на момент документування не містить жодних фреймів чи компонентів. Зарезервований дизайнерами для майбутніх застарілих компонентів, які знімають з активного використання.

## Notes
- Нічого документувати — canvas порожній.
- Якщо в майбутньому тут з'являться компоненти, це означатиме "не використовуй у новому UI, замінено на щось інше" — перевіряй цей canvas, якщо стикаєшся з компонентом, якого немає в жодному з інших канвасів.

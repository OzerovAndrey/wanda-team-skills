# System

Canvas `5:2642`, file `1iLHv8dNR3kwSoBYNl1wgs`. Службові технічні utility-компоненти Figma-файлу — **не UI-елементи продукту**, а внутрішні заглушки, які дизайнери використовують як "тут буде довільний контент" у слот-пропсах інших компонентів.

## System/Node
Node `8:246`. Фіолетовий dashed-бокс (`bg-[rgba(151,71,255,0.08)] border-[#9747ff] border-dashed rounded-[9px]`, 40px) — це той самий плейсхолдер, що ми вже бачили як дефолтне значення десятків `layout`/`component*`-пропсів по всьому файлу (Card/Default, Drawer, Modal, List/Item тощо).

## System/Empty Component
Node `8:245`. Невидимий 0×0px елемент — технічний "порожній" плейсхолдер (напр. коли слот не повинен нічого показувати за замовчуванням).

## System/Stack/2-5
Node `237:74620` та ін. Вертикальний стек із 2/3/4/5 System/Node-елементів — демонструє "тут буде список довільних елементів" (напр. дефолтний вигляд Menu-слоту в Dropdown).

## Notes
- **Це не компоненти для використання в реальному UI** — якщо десь у коді бачиш фіолетовий dashed-прямокутник як фон/рамку, це сигнал "сюди дизайнер очікує підстановку реального контенту", а не частина фінального дизайну.
- Корисно розпізнавати цей паттерн при читанні `get_design_context` виводу інших компонентів — System/Node і System/Stack/N з'являються як дефолтні значення `component`/`layout`-пропсів, і їх не треба відтворювати буквально в реальному коді.

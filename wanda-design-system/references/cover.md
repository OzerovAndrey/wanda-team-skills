# Cover

Canvas `5:2650`, file `1iLHv8dNR3kwSoBYNl1wgs`. **Не продуктовий UI-компонент** — внутрішні службові елементи самого Figma-файлу (обкладинка + статус-мітки для документування прогресу).

## Cover
Node `8:158`. Одна велика (1600×960) обкладинка-заставка файлу — суто декоративна, не використовується в продукті.

## Cover/Badge
Node `8:187`. Variant(Todo/In progress/On review/Stopped/Done/Deprecated) — великі кольорові pill-бейджі (шрифт **Poppins**, третій шрифт у файлі, окрім Bungee/Open Sans — ще одне підтвердження, що це чисто службовий елемент, не частина продуктової типографіки). Використовуються дизайнерами, щоб позначати статус готовності секцій/фреймів прямо на канвасі Figma (типу "цей розділ ще TODO", "цей — Deprecated" тощо).

## Notes
- **Ігноруй цей канвас при документуванні продуктових компонентів** — це мета-інструмент для самого Figma-файлу, не UI-бібліотека.
- Якщо десь у get_metadata бачиш фрейм із назвою на кшталт "TODO"/"Deprecated" великими літерами — це, ймовірно, один із цих Cover/Badge, а не реальний компонент.
